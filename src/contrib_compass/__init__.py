"""
contrib_compass — Find open source repos and issues that match your skills.

This package exposes the FastAPI application via ``contrib_compass.main:app``.

Version follows semantic versioning (MAJOR.MINOR.PATCH).
Bump this in sync with git tags (e.g. v0.1.0 → version = "0.1.0").
"""

__version__ = "0.1.0"
